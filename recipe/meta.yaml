{% set version = "0.0.16" %}

package:
  name: geocube
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/g/geocube/geocube-{{ version }}.tar.gz
  sha256: 75a39c22a238f06634bf337a50f8dcb9f85c0b9f7efbdd46dcd0b7661c91f900

build:
  number: 0
  noarch: python
  entry_points:
    - geocube=geocube.cli.geocube:geocube
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - python 3.6
    - pip
  run:
    - python >=3.6
    - appdirs
    - click >=6.0
    - datacube
    - geopandas >=0.6
    - pyproj >=2
    - rasterio
    - rioxarray >=0.0.30
    - xarray

test:
  imports:
    - geocube
    - geocube.api
    - geocube.geo_utils
    - geocube.xarray_extensions
  commands:
    - pip check
    - geocube --help
  requires:
    - pip

about:
  home: https://github.com/corteva/geocube
  license: BSD-3-Clause
  license_family: BSD
  license_file: LICENSE
  summary: Tool to convert geopandas vector data into rasterized xarray data

extra:
  recipe-maintainers:
    - snowman2
    - ocefpaf
